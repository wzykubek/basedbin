{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: FastAPI",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["basedbin:app", "--reload", "--port", "8080"],
      "envFile": "${workspaceFolder}/.env",
      "jinja": true
    }
  ]
}
